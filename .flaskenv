# Loaded by Flask when python-dotenv is installed: pip install python-dotenv
# Ensures `flask run` uses this project's app.py (fixes 404 when cwd / FLASK_APP is wrong).
FLASK_APP=app.py
FLASK_RUN_HOST=127.0.0.1
FLASK_RUN_PORT=5000
