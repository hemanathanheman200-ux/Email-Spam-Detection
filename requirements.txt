# Python dependencies for this project (SpamGuard / landing page Flask app).
# Encoding: UTF-8 (no BOM). Install with: pip install -r requirements.txt
#
# --- Application ---
# Flask: web framework (routes, templates, sessions). Installs Werkzeug, Jinja2, Click, and related libraries.
Flask==3.1.3
scikit-learn==1.7.2
numpy==2.2.6
python-dotenv==1.2.2

# --- Production server ---
# Gunicorn: WSGI HTTP server for Linux/production (containers, VMs, many PaaS hosts).
gunicorn==26.0.0
